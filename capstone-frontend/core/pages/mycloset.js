import ItemList from '../components/ItemList'
import { Button, ButtonGroup } from '@chakra-ui/react'

export default function MyCloset({ items }) {
    return (
        <div>
            <Button>Add an Item</Button> 
        </div>
    )
}

